# Tasks: Observabilidade

- [x] 1. Criar migration `database/26. audit_log.sql` com tabela + RPCs `log_audit` e `health_check`
- [x] 2. Criar serviço `auditService.ts` com funções `log()` e `healthCheck()`
- [x] 3. Criar componente `ErrorBoundary.tsx`
- [x] 4. Integrar ErrorBoundary + auditService nos dois App.tsx
